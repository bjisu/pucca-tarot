# 뿌까 오라클 — 실행 가이드 (비개발자용)

내 컴퓨터에서 브라우저로 뿌까 오라클을 직접 써보는 방법입니다.

---

## 가장 쉬운 방법 (더블클릭)

1. 프로젝트 폴더(`pucca tarot`)를 엽니다.
2. **`start_server.bat`** 파일을 더블클릭합니다.
3. 검은 창이 뜨고 `Uvicorn running on http://127.0.0.1:8000` 이라는 글자가 보이면 성공!
4. 크롬(또는 엣지) 브라우저를 열고 주소창에 아래를 입력합니다.

   ```
   http://localhost:8000
   ```

5. 뿌까 오라클 홈 화면이 나오면 끝! 검은 창은 닫지 말고 그대로 두세요 (닫으면 서버가 꺼집니다).
6. 다 쓰고 나면 검은 창을 닫으면 됩니다.

---

## 터미널로 직접 실행하는 방법

1. 프로젝트 폴더에서 주소창에 `cmd` 라고 입력하고 Enter → 명령창이 열립니다.
2. 아래를 한 줄 입력하고 Enter:

   ```
   .venv\Scripts\python.exe -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
   ```

3. 브라우저에서 `http://localhost:8000` 접속.
4. 끌 때는 명령창에서 `Ctrl + C`.

> 처음 받은 컴퓨터에서 `.venv` 폴더가 없다면 (최초 1회만):
>
> ```
> python -m venv .venv
> .venv\Scripts\python.exe -m pip install -r requirements.txt
> ```

---

## AI 해석(LLM) 켜는 방법 — 선택사항

지금 상태로도 모든 기능이 동작합니다. 다만 API 키가 없으면 카드에 저장된
기본 의미로 만든 **내장 해석**이 나옵니다. 뿌까가 질문에 딱 맞춰 말해주는
진짜 AI 해석을 보려면:

1. 프로젝트 폴더의 **`.env`** 파일을 메모장으로 엽니다.
2. 갖고 있는 API 키를 아래 셋 중 하나에 붙여넣습니다 (하나만 있어도 됩니다).

   ```
   ANTHROPIC_API_KEY=여기에_키_붙여넣기
   OPENAI_API_KEY=
   GEMINI_API_KEY=
   ```

3. 저장한 뒤 서버를 껐다가(`Ctrl+C` 또는 창 닫기) 다시 켭니다.
4. 키는 절대 다른 곳에 공유하거나 git 에 올리지 마세요. (`.env`는 자동으로 git에서 제외됩니다)

---

## 이미지 넣는 방법 — 선택사항

지금은 이미지가 없어서 카드 이름이 적힌 보라·금색 플레이스홀더가 표시됩니다.
이미지 파일만 넣으면 **자동으로** 적용됩니다.

- **카드 이미지 (78장)**: `assets/cards/` 폴더에 넣기.
  파일명은 `assets/cards/README.md`의 규칙을 따르세요.
  (예: `major_00_the_fool.png`, `wands_01_ace.png` — `data/tarot_cards.json`의 `image` 값과 동일해야 함)
- **뿌까 이미지**: `assets/character/pucca_main.png` 로 저장.

넣은 뒤 브라우저에서 새로고침(F5)만 하면 됩니다.

---

## 문제가 생기면

| 증상 | 해결 |
|---|---|
| 브라우저에 "사이트에 연결할 수 없음" | 검은 창(서버)이 켜져 있는지 확인 → `start_server.bat` 다시 실행 |
| "서버와 연결할 수 없어" 화면 | 서버를 다시 켜고 화면의 "다시 시도" 버튼 클릭 |
| 8000 포트를 이미 사용 중이라는 오류 | `start_server.bat` 파일을 메모장으로 열어 `8000`을 `8080` 등으로 바꾸고, 브라우저 주소도 `http://localhost:8080` 으로 접속 |
| 해석이 매번 비슷하게 나옴 | `.env`에 API 키가 비어 있어 내장 해석 모드로 동작 중 → 위 "AI 해석 켜는 방법" 참고 |
